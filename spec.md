# Specification

## Summary
**Goal:** Replace the compliance email address with the support email address across all frontend source files.

**Planned changes:**
- Replace all occurrences of `compliance@stackteacher.com` with `support@stackteacher.com` in all frontend source files.

**User-visible outcome:** Any location that previously displayed `compliance@stackteacher.com` will now show `support@stackteacher.com`.
